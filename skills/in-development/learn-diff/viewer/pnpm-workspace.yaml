# pnpm 10+ บล็อก postinstall script โดย default — esbuild (ตัว bundler ของ vite)
# ต้องได้รันเพื่อวาง binary ให้ตรง platform ไม่งั้น `vite` พังตอนรัน
# pnpm 11 ใช้ allowBuilds เพื่ออนุญาต build script แบบ explicit
allowBuilds:
  esbuild: true
